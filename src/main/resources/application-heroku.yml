spring:
  application:
    name: real-estate-manager

  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:real

logging:
  level:
    sql: DEBUG
    root: WARN
    projects.realestatemanager: DEBUG